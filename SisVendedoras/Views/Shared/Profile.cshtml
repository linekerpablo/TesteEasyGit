@using SisVendedoras.Authentication.Authentication
<li class="dropdown user user-menu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <img src="~/assets/img/logo.jpg" class="user-image" alt="User Image">
        @if (AuthenticationUser.UserSession != null)
        {
            <span class="hidden-xs">@AuthenticationUser.UserSession.Nome</span>
        }
    </a>
    <ul class="dropdown-menu">
        <!-- User image -->
        <li class="user-header">
            <img src="~/assets/img/logo.jpg" class="img-circle" alt="User Image">
            <p>
                @if (AuthenticationUser.UserSession != null)
                {
                    @AuthenticationUser.UserSession.Nome
                    <small>@AuthenticationUser.UserSession.Email</small>
                }
            </p>
        </li>
        <!-- Menu Body -->
        @*<li class="user-body">
                <div class="row">
                    <div class="col-xs-4 text-center">
                        <a href="#">Followers</a>
                    </div>
                    <div class="col-xs-4 text-center">
                        <a href="#">Sales</a>
                    </div>
                    <div class="col-xs-4 text-center">
                        <a href="#">Friends</a>
                    </div>
                </div>
            </li>*@
        <!-- Menu Footer-->
        <li class="user-footer">
            @*<div class="pull-left">
                    <a href="#" class="btn btn-default btn-flat"><i class="fa fa-user"></i>&nbsp;Perfil</a>
                </div>*@
            <div class="pull-right">
                <button class="btn btn-default btn-flat" type="submit" ng-click="loggout();"><i class="fa fa-sign-out"></i>&nbsp;Sair</button>
            </div>
        </li>
    </ul>
</li>